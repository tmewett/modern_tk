[project]
name = "modern_tk"
version = "0.1"
dependencies = [
    "msgpack",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/modern_tk"]
